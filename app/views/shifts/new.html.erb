<h1>New Shift</h1>
<%= form_for :shift, url: shifts_path do |s| %>

<% if @shift.errors.any? %>
<div id="error_explanation">
    <h2>
        <%= pluralize(@shift.errors.count, "error") %> prohibited this shift from being saved:
    </h2>
    <ul>
        <% @shift.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
        <% end %>
    </ul>
</div>
<% end %>

    <p>
        <%= s.label :hours %><br>
        <%= s.text_field :hours %>
    </p>
    
    <p>
        <%= s.submit %>
    </p>
    <% end %>
    <%= link_to 'Back', shifts_path %>